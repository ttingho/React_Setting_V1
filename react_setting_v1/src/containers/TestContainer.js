import React from 'react';
import TestTitle from 'components/TestTitle';

// 비즈니스 로직을 작성하는 공간 입니다. 여기서 components 폴더에 있는 컴포넌트들을 불러와 사용합니다.
// 또한 components 폴더의 컴포넌트들은 뷰 로직에 해당하는 컴포넌트들로만 만들어 작업합니다.
const TestContainer = () => {
  return (
    <TestTitle />
  );
};

export default TestContainer;
